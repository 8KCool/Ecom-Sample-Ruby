Crie sua loja!
<%= form_for @store, html: {multipart: true}  do |f| %>
    <%= f.label :name%>   
    <%= f.text_field :name%>


    <%= f.label :slogan%>
    <%= f.text_field :slogan%>

    <%= f.submit "Enviar", class: "btn btn-primary"%>
<% end %>
